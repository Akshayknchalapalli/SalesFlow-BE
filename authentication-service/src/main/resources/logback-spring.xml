<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOGS" value="./logs" />

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %d{ISO8601} %highlight(%-5level) [%blue(%t)] %yellow(%C{1}): %msg%n%throwable
            </Pattern>
        </layout>
    </appender>

    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/auth-service.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d %p %C{1} [%t] %m%n</Pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOGS}/archived/auth-service-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>
    
    <!-- Tenant-related logging with higher verbosity -->
    <logger name="com.salesflow.auth.tenant" level="DEBUG" />
    <logger name="com.salesflow.auth.filter.TenantDebugFilter" level="DEBUG" />
    <logger name="com.salesflow.auth.filter.TenantValidationFilter" level="DEBUG" />
    <logger name="com.salesflow.auth.filter.TenantContextFilter" level="DEBUG" />
    
    <!-- Spring web logging -->
    <logger name="org.springframework.web" level="INFO" />
    
    <!-- Swagger/OpenAPI logging -->
    <logger name="org.springdoc" level="INFO" />
    
    <!-- Security-related logging -->
    <logger name="org.springframework.security" level="INFO" />
    
    <!-- Authentication service logging -->
    <logger name="com.salesflow.auth" level="INFO" />
    
    <!-- Turn off noisy logs -->
    <logger name="org.apache.catalina" level="WARN" />
    <logger name="org.hibernate" level="WARN" />
    <logger name="com.netflix" level="WARN" />
    
    <!-- SQL logging only in dev environment -->
    <springProfile name="dev">
        <logger name="org.hibernate.SQL" level="DEBUG" />
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />
    </springProfile>

    <root level="INFO">
        <appender-ref ref="Console" />
        <appender-ref ref="RollingFile" />
    </root>
</configuration>